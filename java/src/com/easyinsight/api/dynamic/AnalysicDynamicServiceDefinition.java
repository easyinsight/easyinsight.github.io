package com.easyinsight.api.dynamic;

import com.easyinsight.analysis.WSAnalysisDefinition;

/**
 * User: James Boe
 * Date: Sep 8, 2008
 * Time: 12:19:32 AM
 */
public class AnalysicDynamicServiceDefinition {
    public void generateCode(WSAnalysisDefinition analysisDefinition) {
        // public Order[] getOrders() {
        //    ListDataResults results = new ParamaterizedDataService().list(analysisDefinition, false);
        //    Row[] rows = results.getRows();
        //    AnalysisItem[] headers = results.getHeaders();
        //    for (IRow row : dataSet.getRows()) {
        //        Order order = new Order();
        //        order.setOrderAmount(row.getValue(orderAmountKey));
        //        order.setCustomer(row.getValue(customer));
        //    }
        // }
    }
}
