<project default="EasyInsightDatabaseSolution" basedir="." name="EasyInsightDatabaseSolution">
    <property file="../../flex/build/local.build.properties"/>

    <property name="out.path" value="../out"/>
    <property name="out.classes.path" value="../out/classes"/>
    <target name="EasyInsightDatabaseSolution">
        <mkdir dir="${out.path}"/>
        <mkdir dir="${out.classes.path}"/>
        <javac destdir="${out.classes.path}"
               optimize="off"
               debug="on" failonerror="false"
               srcdir="../src"
               excludes="**/*.smap">
            <classpath>
                <fileset dir="../lib">
                    <include name="**/*.jar"/>
                </fileset>
                <fileset file="../../java/lib/core/servlet-api.jar"/>
            </classpath>
            <include name="**"/>
            <exclude name="tags/**"/>
        </javac>
        <war destfile="${out.path}/eidbsolution.war">
            <lib dir="../lib" />
            <classes dir="${out.classes.path}" />
            <fileset dir="../web" />
            <classes file="../src/hibernate.cfg.xml" />
        </war>
        
    </target>
</project>